# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

1.0.0 未満のため、minor の更新に破壊的変更が含まれることがあります。

## [0.16.0] - 2026-08-21

### 変更（受け取り側の挙動）

- **届いた畳みを LWW で見送るようになりました。** ローカルの敗者行が畳みより後に更新
  されていれば、その畳みは適用されません。これまでは削除時刻を見ずに必ず適用していた
  ため、畳みのあとにユニークキーごと書き換えられて**もう衝突しない行**まで消えていました。
  見送ってもデータは失われず、その行を送り返した時点で相手側が逆向きに収束させます。
- **`deleteProtected` は畳みを保護しなくなりました。** 利用者操作による削除だけを止めます。
  畳みはユニーク制約が強制する統合であり、見送っても行は救えない（勝者行が届いた時点で
  `applyInsert` が同じ統合を行うだけで、それまでのあいだ子が宙に浮く）ためです。

どちらも旧バージョンのクライアントと混在して動作します。

### 修正

- 畳みの DELETE が、生まれた直後の changelog 掃除で消えることがありました。敗者 id に
  保持期間より古い削除履歴があると、その時刻で記録されて同じ `performSync` の
  `cleanupChangelog` に刈られ、`_id_merge` には記録が残るため二度と書き直されませんでした。
- `_changelog` / `_tombstone` が揃う前に `applyInsert` が使われたデータベースで、畳みが
  以降ずっと差分経路に載らなくなる問題を修正しました。
- 削除が実際には起きていない経路（`local_wins`）の tombstone に現在時刻を刻んでいたため、
  その id の到着が無条件に止まっていました。勝者行のタイムスタンプを刻むようにしました。
- フルマージ中（トリガー無効）に起きた畳みが差分経路に載らず、フルマージでしか他の
  クライアントへ渡らない問題を修正しました。主キーが動く 1:1 テーブルの付け替えも同様です。
- `_tombstone.deletedAt` の大小比較を `julianday` 正規化に統一しました（ISO-T 形式と
  スペース形式が混在するため）。

### パフォーマンス

- `PRAGMA foreign_key_list` / `table_info` の結果を `PRAGMA schema_version` をキーに
  データベース接続ごとにキャッシュするようにしました。畳みが一度でも起きたデータベースでは
  `applyInsert` のたびに外部キーを走査し、畳み 1 回ごとに全テーブルへ pragma を投げていました。

### 内部

- TypeScript 7 に備えて `module` / `moduleResolution` を `node16` へ移行しました
  （`node10` は TypeScript 6 で deprecated）。出力は CommonJS のままです。
- `package.json` に `type: "commonjs"` と `exports` を追加しました。`exports` の追加により
  deep import（`sqlite-nas-sync/dist/*`）は解決されなくなります。公開 API は
  `sqlite-nas-sync` からの import のみです。
- `npm run typecheck`（`tsconfig.test.json`）でテストコードも型検査するようにしました。

## [0.15.0] - 2026-08-20

- 畳み先を `_tombstone.mergedInto` に載せて他のクライアントへ伝播させるようにしました。
  競合を経験していないクライアントが、敗者行を消す前に子を畳み先へ引き取れます。
- 勝った側（`local_wins`）からも畳み先が通常の差分経路で届くようにしました。

## [0.14.0] - 2026-08-20

- 競合で負けた行を指している子を、勝った行へ引き取るようにしました（先に削除すると
  カスケードで子が道連れになっていました）。

## [0.13.1] - 2026-06-24

- INSERT / UPDATE の LWW 比較を `julianday` 正規化に統一しました。

## [0.13.0] - 2026-06-24

- tombstone を優先する LWW 削除統合と、リモートデータベースからの削除時刻解決を追加しました。

## [0.12.0] - 2026-06-12

- 競合解決 API（`applyInsert` / `applyUpdate` / `applyDelete`）を公開しました。

## [0.11.0] - 2026-06-11

- セカンダリ UNIQUE 違反を LWW で解決するようにしました。

## [0.10.0] - 2026-06-11

- `SyncResult` に `skippedRemotes` を追加しました。

## [0.9.0] - 2026-06-11

- リモートデータベースをローカルコピー経由で読むようにし、`lastSeenId` の更新を
  原子的にしました。

## [0.8.0] - 2026-04-11

### 破壊的変更

- テーブル自動検出 API に刷新しました（`discoverTables`）。

## [0.7.0] - 2026-03-27

- tombstone + heartbeat + フルマージによる zombie 問題の解決を追加しました。

## [0.6.1] - 2026-03-27

- stale なクライアントが NAS を汚染する問題を修正しました（pull-first フロー）。

## [0.6.0] - 2026-03-24

- コード構造をモジュールごとに整理しました。

## [0.5.0] - 2026-03-24

- `schemaVersion` 未指定時に、テーブルスキーマからハッシュを自動生成するようにしました。

## [0.4.0] - 2026-03-24

- `schemaVersion` オプションによるスキーマバージョン互換チェックを追加しました。

## [0.3.1] - 2026-03-23

- `better-sqlite3` を `peerDependencies` へ移しました。

## [0.3.0] - 2026-03-23

### 破壊的変更

- テーブルごとの設定（`timestampColumn` / `deleteProtected`）と `onAfterSync`
  コールバックを追加しました。

## [0.2.0] - 2026-02-07

- 最初のリリース。`_changelog` テーブルと SQLite トリガーによる差分同期。
