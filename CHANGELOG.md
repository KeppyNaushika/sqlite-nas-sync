# 変更履歴

このファイルの書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョン番号は [Semantic Versioning](https://semver.org/lang/ja/) に従います。

1.0.0 未満のため、minor の更新に破壊的変更が含まれることがあります。

## 未リリース

### 修正

- **畳みの記録に、同期を回した時刻を刻んでいました。** 別id・同一ユニークキーの2行を
  1行へ畳むとき、`_tombstone.deletedAt` と `_id_merge.mergedAt` には**現在時刻**が
  入っていました。実データの `updatedAt` は必ずそれより過去なので、畳まれた側の id は
  「削除済みより古い挿入」として**無条件に止まります**。

  畳みは削除ではなく「この2行は同じものだ」という判断です。止めてよいのは**その判断が
  見ていた版まで**であって、それより新しい版まで止めてよい理由はありません。実際、
  次のどれもが黙って落ちていました。

  - 畳みが決まったあとに、より新しい敗者版を持っていた端末の変更（**その端末の版が
    世界で最新でも**負ける）
  - 敗者行のユニークキーがその後変更され、もう衝突しなくなった行（生き返れない）
  - 同じ1回の畳みなのに、敗者行を消した端末と消さなかった端末で違う時刻が刻まれる
    （消した側は現在時刻＝最強のしきい値になる）

  刻む時刻を**勝った行の `updatedAt`** に統一しました。畳みの記録は、行と同じLWWの
  下に置かれます。利用者操作による**本物の削除**は従来どおり現在時刻です
  （`foldedAt` の有無が両者を分ける境目で、畳みは「新しい方へ進める」のではなく
  **置き直します** — 敗者行を消した側では DELETE トリガが先に現在時刻を書いており、
  進めるだけでは畳みの時刻が必ず負けるためです）。

- **畳みを適用しただけで、判断の時刻が「今」へ繰り上がっていました。** 他端末が下した
  畳みを取り込むとき、ローカルの記録には現在時刻が入っていました。畳みを適用した端末が
  増えるたびにしきい値が現在へ寄り、まだ届いていない新しい版が捨てられる範囲が
  広がります。受け取った判断の時刻をそのまま刻むようにしました。

- **子どうしの畳み（付け替え先に「同じもの」が既に在る場合）も、同じく現在時刻を
  刻んでいました。** さらに、時刻を読む列名に**親の**設定をそのまま使っていたため、
  子テーブルの列名が違う場合は値が取れず、黙って現在時刻に落ちていました
  （子テーブルの列名を解決してから読むようにしました）。

- **畳まれて消えた行を指す外部キーの向け直しが、記録より新しい敗者行にも及んでいました。**
  向け直すのは、記録が**ローカルの敗者行**より新しいときだけになりました。敗者行が記録
  より新しく更新されていれば、その畳みはもう古い判断です。

- **鎖を辿って畳み先が動いたのに、動く前の勝者行を入れていました。** 届いた
  `A → C` に対してローカルが既に `C → B` を持っている場合、畳み先は終端の `B` へ
  解決されるのに、入れ替えに使う行は呼び出し元が `C` で読んだままでした。結果、
  **この端末が既に畳んで tombstone まで書いた `C` が復活**し、その子は行き先の無い
  id へ読み替えられて捨てられます。終端と手元の行が食い違う場合は畳みを見送るように
  しました（敗者行を消さなければ子は道連れになりません）。

- **受け取った畳みの時刻を捨て、勝者行の「今の」時刻でしきい値を作っていました。**
  敗者も勝者もローカルに無い端末が畳みを取り込むとき、`_tombstone.deletedAt` には
  勝者行が**現在名乗っている**時刻が入っていました。判断のあとに勝者が編集されていると、
  その間に居る敗者idの版がこの端末でだけ黙って捨てられ、判断を下した端末との間で
  **永久に食い違います**。受け取った `deletedAt` をそのまま刻むようにしました。

- **`_id_merge.mergedAt` が巻き戻ることがありました。** 同じ畳みが古い時刻を名乗って
  二度届くと、記録だけが過去へ戻り（`_tombstone.deletedAt` の側は巻き戻りません）、
  その間に居る敗者行が「記録より新しい」と見えて外部キーの向け直しが止まります。
  遅れて届いた子は存在しない親を指したまま入り、**その相手ぶんの取り込みが丸ごと
  巻き戻ります**（同期がその相手から止まる）。tombstone と同じく遅い方を採ります。

- **循環へ流れ込む鎖が、起動時の掃除1回では畳み切れませんでした。** `D→A` があり
  `A↔B` が循環している場合、1回目は終端が循環の中に居るため張り替えられず、循環を
  刈った結果 `D→A→B` が残ります。読み替えは1段しか辿らないので、`D` の子は既に
  死んだ `A` へ向けられて捨てられます。**何も変わらなくなるまで**走査するようにしました。

- **NULL を含む複合外部キーを「親が居ない」と判定し、行を捨てていました。** SQLite は
  子の列が1つでも NULL なら、その参照を検査しません（＝`ON DELETE` の動作も及ばない）。
  それを全列 `列 = ?` で引いていたため、`= NULL` が真にならず「親が消えている」と
  判断され、**SQLite ならそのまま通る行**が捨てられていました。

- **親の表を、子の時刻列で引いていました。** 表ごとに時刻列を変えられる設定
  （`TableConfig.timestampColumn`）で、親が `modifiedAt`・子が既定の `updatedAt` の場合、
  「親が作り直されたか」と「畳みの記録がまだ有効か」の判定が列を見つけられず、
  作り直された親を認識できないまま子を捨てていました。表ごとの設定を引くようにしました。

- **同時刻かどうかを、字面で見ていました。** 膠着（どちらも勝てない食い違い）の判定が
  文字列一致だったため、ISO-T形式とスペース形式が混在する端末どうしでは同時刻に
  気づけず、報告されないまま黙って捨て合っていました。他と同じく `julianday()` で
  正規化して比べます。

- **畳み先を張り替えると、畳みの時刻が未来へ動いていました。** `A→B` のあとに
  `B→C` が来ると、`A` の向き先を `C` へ張り替えるついでに `_id_merge.mergedAt` まで
  新しい方（`B→C` の時刻）へ進めていました。対になる `_tombstone.deletedAt` は
  `A` 自身の畳みの時刻のままなので、2つの帳簿が食い違います。実測では、1月に畳まれた
  `A` が6月の巻き添えで `_id_merge` だけ6月になり、3月版の `A` は tombstone（1月）を
  越えて**復活する**一方、畳みの記録（6月）はまだ有効と見なされて**その子だけ `C` へ
  読み替えられました**。向き先が変わっても「`A` が畳まれた時刻」は動きません。

- **畳めなかったのに、帳簿にだけ「畳んだ」と書いていました。** 鎖を辿って畳み先が
  動き、その終端の行が手元に無いときは畳みを見送りますが、見送る前に
  `_id_merge` と `_tombstone.mergedInto` へ記録していました。敗者行は生きたまま
  「畳まれた」と記録され（しかも `_tombstone` は同期で他端末へ伝わります）、遅れて
  届いたその子だけが手元に無い終端へ読み替えられて本当の親から切り離されます。
  記録も見送るようにしました（リモートの `_tombstone` は毎回全件読み直すので、
  終端の行が届いた次の同期で同じ畳みがやり直されます）。

- **循環した畳みの記録を刈るとき、`_tombstone.mergedInto` を置き去りにしていました。**
  `_id_merge` からは消えるのに tombstone は畳み先を名乗り続けるため、外部キーの
  読み替えが止まり、遅れて届いた子が消えた親を指したまま入って COMMIT 時の外部キー
  検査でその相手ぶんの取り込みが丸ごと巻き戻ります。鎖の畳み直し・循環の刈り取りの
  どちらでも、2つの帳簿を揃えて動かすようにしました。

- **届いた行を採らなかったのに `upserted` と名乗っていました。** 同一PKでローカルが
  勝った場合、リモートの行は書かれていないのに `applyInsert` は `upserted` を返し、
  同期結果では `conflictsResolved` に数えられていました（`applyUpdate` は同じ結果を
  `skipped` と呼びます）。呼び方を揃え、INSERT 側でも `skipped` と畳みを
  UPDATE 側と同じ数え方にしました。

- **時刻列の設定がずれていると、膠着の警告が行の数だけ出ていました。** 両側とも
  時刻を読めず（空文字）「同時刻」と判定されるためです。明示的な `tables:` 設定で
  実在しない列を指したときにだけ起きます。時刻が読めていないときは膠着ではないので
  黙ります。

- **`npm run build` が `dist/` を消していませんでした。** 分割前のビルドが残した
  `dist/conflict.js` があると、CommonJS の解決が `./conflict/index.js` より
  そちらを先に拾い、**削除したはずの旧実装が公開されます**（型検査もテストも通った
  ままで）。`build` の前に `dist/` を消すようにしました。

### 変更

- **`src/conflict.ts`（3000行超）を役割ごとに分割しました。** 公開APIと import 先
  （`./conflict`）は変わりません。`conflict/schema`（`PRAGMA` の読み取り）、
  `conflict/timestamp`（時刻の比較と決め事）、`conflict/unique`（ユニークキーと衝突相手）、
  `conflict/ledger`（`_id_merge` / `_tombstone` / `_changelog` への記録）、
  `conflict/remap`（外部キーの向け直し）、`conflict/fold`（行を1つへ畳む）、
  `conflict/stalemate`（膠着の報告）、`conflict/apply`（適用の入口）。

### 追加

- **向け直した先が既に消えているとき、`ON DELETE` の宣言に従うようになりました。**
  畳み先がそのあと利用者に消されたあとで敗者の子が届くと、外部キー違反で**その相手
  ぶんの取り込みが丸ごと巻き戻り、同期がその相手から永久に止まって**いました（しかも
  `syncNow` は成功として返ります）。「その子が手元に居たら何が起きていたか」を再現します。

  | `ON DELETE` | 扱い |
  | --- | --- |
  | `CASCADE` | その子を採らない |
  | `SET NULL`（対象列がすべて nullable） | その外部キーの**全列**を `NULL` にして採る |
  | `SET NULL`（`NOT NULL` 列を含む） | 採らずに警告 |
  | `SET DEFAULT`（既定値の親が居る／既定値に `NULL` を含む） | その外部キーの**全列**を既定値にして採る |
  | `SET DEFAULT`（既定値の親が居ない） | 採らずに警告 |
  | `RESTRICT` / `NO ACTION` | 採らずに警告 |
  | 外部キーが子の**主キー**を兼ねる場合の `SET NULL` / `SET DEFAULT` | 採らずに警告 |

  `SET DEFAULT` は SQLite の実動作を測って合わせました。SQLite は子の外部キー列を
  **宣言された既定値**にし（複合外部キーでも全列。宣言の無い列は `NULL`）、その既定値の
  親が在れば子は生き残り、無ければ削除そのものが外部キー違反で失敗します。
  `PRAGMA table_info` の `dflt_value` は**SQLの字面**（`DEFAULT 'x'` ならクォート込みの
  `'x'`、式なら式そのもの）なので、そのまま列へ入れず SQLite に評価させています。

  判断が働くのは**向け直しが起きた参照**で、しかも消えたと分かっている場合だけです。
  まだ届いていないだけの親は今までどおり扱います。`_tombstone` に載っていても
  **取り込み元にその行が現存すれば「作り直された」とみなして子を採ります** —
  tombstone は「いつか消された」の記録であって「今も消えている」ではないためで、
  これが無いと同じ取り込みの中で親が作り直されるときに**子が先に処理された場合だけ
  子が捨てられる**という順番依存になります（実測）。
  1つの表が `CASCADE` と `SET NULL` の親を両方持つ場合は、採らないと決めた親が1つでも
  あれば採りません（外部キーを見る順番で結果は変わりません）。

- **見送った行・列を外した行が `SyncResult.warnings` に載るようになりました。**
  `applyInsert` / `applyUpdate` / `applyMergedDelete` の戻り値にも `warnings` が
  加わります。黙って捨てません。

- **読み替えても、行の時刻には触らなくなりました。** 向け直しで書き換わるのは外部キーの
  列だけで、他の列は元の書き手のものです。行の時刻は1つしか無いので、行全体で畳みの時刻を
  名乗ると外部キー以外の列について過大に申告することになり、その分だけ
  「元の時刻〜畳みの時刻」の窓に入る**他端末の本物の編集が消えます**。実測では、編集が
  消えたうえに同じ時刻で内容が割れて**永久に食い違い**ました。

  名乗れば解ける食い違い（同じ子を両端末が別々の親へ繋いだまま同じ時刻で持ち合う形）は
  ありますが、**書かれたデータを消してまで解くものではない**と判断し、解かずに
  **報告する**ようにしました（下記）。親の付け替えを子へ波及させたい場合は、アプリ側で
  子の `updatedAt` に触ってください。

- **解けない食い違いを報告するようになりました。** 同一主キー・同時刻で中身が割れている
  （どちらも「厳密に新しくない」ので互いに拒み続ける）状態を検出し、
  `SyncResult.warnings` に出します。

  ```
  Stalemate on exam_students:es-1: both sides are at 2026-05-01T00:00:00Z
  but studentId differ, so neither can win. Edit the row on one side to break the tie.
  ```

  どちらが正しいかはドメインの意味で決まるのでライブラリは解きません（勝手に選べば必ず
  片方の編集が消えます）。判定が走るのは**時刻が同じで採らないと決めた経路だけ**で、
  読み込み済みの2行を突き合わせるだけなので**データベースへの問い合わせは増えません**（実測）。
  差のある列が多い表では先頭5列だけを挙げ、残りは件数へ畳みます（実測: 40列で844文字→231文字）。

- **README に「ライブラリが保証すること／しないこと／アプリが決めること」を追加しました。**
  保証するのは「決定論的に収束する」「書かれたデータを黙って壊さない」「解決できないことは
  報告する」の3つで、ドメインの意味（同一性の判断、親の付け替えを子へ波及させるか）は
  アプリの責務です。

- **届いた畳み先が、こちらでは既に別の行へ畳まれている場合を辿るようにしました。**
  `_tombstone.mergedInto` は同期で他の端末へ渡るため、`A→C` と `C→B` の鎖がそのまま
  届きます（実測）。中間の `C` は既に死んでいるので、そのまま使うと「畳み先が見つからない」
  と判断して**敗者行を畳めず、その行と子が端末間で食い違ったまま**残っていました。

- **起動時に、旧バージョンが残した畳み先の鎖を畳むようにしました**（`setupChangelog`）。
  鎖が残っていると、遅れて届いた子が**既に死んでいる中間の行**へ向けられ、`ON DELETE` に
  従って捨てられます。時刻は動かしません（`A→C` を `A→B` へ張り替えても「`A` が畳まれた
  時刻」は変わらないため）。何度走らせても結果は同じです。

  `_id_merge` は**同期されないローカル索引**（`_` 始まりで自動検出から外れ、リモートからも
  読みません）なので、**自分のDBを一度直せば、他の端末が古いライブラリを積んでいても鎖は
  入ってきません**。したがって起動時の一回で足ります。

  矛盾した記録（`A→B` と `B→A` が同時に立つ循環。畳む向きが反転したときに旧バージョンが
  残しえた形）は、**いちばん新しい主張だけを残します**。

### 直っていないこと

README の「直っていないこと（既知）」に一覧があります。要点だけ:

- 畳み先がどこにも無いとき、敗者行は消えない（その行の存在が端末間で食い違う）
- 親の畳みが無効になっても、子の畳みは無効にならない
- 同一主キー・同時刻の純粋な同点
- `SET NULL` はユニーク制約を実質無効にする（SQLiteの性質）

## [0.19.0] - 2026-08-23

### 修正

- **ライブラリが記録する時刻が秒に切り捨てられていて、同じ秒の中で起きた削除と更新の
  前後が失われていました。** `_tombstone.deletedAt` / `_changelog.changedAt` /
  `_id_merge.mergedAt` / `_sync_state.lastSyncedAt` はすべて SQLite の
  `datetime('now')` で書いており、これは秒精度のスペース形式（`2026-05-02 02:19:56`）を
  返します。一方アプリが書く `updatedAt` はふつうミリ秒まで持つ（`toISOString()` 等）ため、
  **削除側だけが粗い**状態でした。

  例: 12:00:00.800 の削除が `12:00:00`（= .000）として記録されるので、その **前** に
  起きた 12:00:00.400 の更新の方が新しいと判定され、**消したはずの行が復活します**
  （実測: 削除の直後に、削除より前の `updatedAt` を持つ更新を適用すると行が戻った）。
  同じ秒の中では削除が最大 999 ミリ秒ぶん過去へずれるので、この向きの取りこぼしだけが
  起こります。

  記録する時刻を `strftime('%Y-%m-%dT%H:%M:%fZ','now')` に変えて、`updatedAt` と
  同じ精度・同じ書式（`2026-05-02T02:19:56.111Z`）で書くようにしました。

  既に在るDBのために2点を合わせています:

  - **トリガを作り直します。** トリガは `CREATE TRIGGER IF NOT EXISTS` で作るため、
    既存DBでは中身が古いまま残り、時刻の精度を上げても秒のままになります。定義を見て、
    古い書き方をしているものだけ作り直します（新しい定義なら何もしないので、起動の
    たびにスキーマが動くことはありません）。あわせてトリガは `changedAt` を明示的に
    書くようにしました（列の `DEFAULT` は既存テーブルには足せないため）。
  - **古い書式の行と混在しても前後は正しく決まります。** 比較はすべて `julianday()` で
    正規化しており、`_changelog` の保持期間の判定（掃除と
    フルマージの取り込み範囲）も文字列比較から `julianday()` 比較へ直しました
    （文字列のままでは、同じ日でも ' '(0x20) < 'T'(0x54) で古い書式の側が常に小さく出ます）。

## [0.18.0] - 2026-08-22

### 変更

- **畳む相手を、例外の文面からではなく索引から先に全部引くようにしました。**
  `PRAGMA index_list` でユニーク索引を数え、`PRAGMA index_xinfo` で列の組と照合順序を
  引きます。SQLite が告げるのは最初にぶつかった 1 本だけなので、それに頼るかぎり
  「1 本目を畳んでから 2 本目の相手が見える」順序から逃げられませんでした。

  これに伴い、0.17.0 の**区切り（SAVEPOINT）の中で畳み、負けが分かった時点で投げて
  そこまでの畳みごと巻き戻す**方式（内部例外 `UpdateRejectedByRival` と再試行ループ）は
  **取り除きました**。1 つも畳む前に全員ぶんの勝敗が決まるので、巻き戻す必要がそもそも
  ありません。1 人でも勝てない相手が居れば何も畳まずに拒みます。

  ユニークの宣言はスキーマそのものから読むので、設定で重ねて教える必要はありません。
  索引の一覧は `PRAGMA schema_version` が変わるまで使い回します。
  `name COLLATE NOCASE` のように照合順序つきで張られた索引も、宣言どおりの照合順序で
  引きます（列の既定の照合順序で引くと相手を取り逃がします）。

- **部分索引（`CREATE UNIQUE INDEX … WHERE …`）と式索引（`… ON t(lower(name))`）は
  「先に数える」対象から外しました。** 列の値だけでは相手を引けないためで
  （前者はどの行が索引に載っているか述語を評価しないと決められず、後者は引くべき値が
  列に無い）、残った違反は畳まずにそのまま例外として返します。誤った相手を畳んで行を
  失うより、止まって知らせる方を採ります。

### 追加

- **`RecordFold.movedChildren`** — その畳みで、消えた行から残った行へ**付け替えた
  直接の子の行数**。「2 つを 1 つにまとめました」だけでは影響範囲が伝わらないので、数を
  添えられるようにしました（例:「小計『知識・技能』を 1 つにまとめ、設問 47 件を
  付け替えました」）。孫は数に入らず、同じ行を二度数えないので**一覧を合計してよい**値です。
- **`RecordFold.lostChildren`** — その畳みで、残った行へ**引き継げずに失われた**直接の子の
  行数。ふつうは 0 で、0 でなくなるのは下記「主キー以外のユニーク列を指す外部キー」の
  修正で**なお守り切れない形**（参照列が `NOT NULL` など）だけです。

### 修正

- **外部キーが子の主キーを兼ねる 1:1 の表（親の id を共有する表）で、畳みが主キー違反を
  投げて同期が永久に止まっていました。** 親が畳まれると子の id そのものが動きますが、
  動いた先の席を別の行が既に占めていることがあります。この先客は**畳みの候補にできません**
  — 畳みは「敗者 id の行を消して勝者 id へ寄せる」ことなのに、先客の id は付け替え後の
  自分の id そのものなので、敗者 id と勝者 id が同じになるためです。畳みは何もせずに戻り、
  そのあと同じ付け替えをもう一度走らせて `SQLITE_CONSTRAINT_PRIMARYKEY` を投げていました。
  この例外はどの catch にも捕まらず、その相手ぶんの取り込みが丸ごと巻き戻ります。

  席は 1 つしか無いのだから、**どちらが勝っても残る行はその席の行 1 つ**です。勝敗が
  決めるのは中身であって、どちらの行が消えるかではありません。勝った方の中身を席の行へ
  移し、動かす側の行を席へ畳むようにしました（席の行を消して入れ直さないので、その行の
  子も道連れになりません）。内部の畳み関数は「畳んだかどうか」を返すようになり、
  畳めていないのに先へ進むことがなくなりました。

- **主キー以外のユニーク列を指す外部キー（`REFERENCES parent(code)` の形）で、畳みのたびに
  子が黙って消えていました。** 「消える行と残る行で参照先の値が同じなら、子はもう残る行を
  指している」として付け替えを飛ばしていましたが、**残る行はまだその値を持っていません**
  — 書き込み（UPDATE / INSERT）は畳みの**あと**に走るためです。子は消える行に繋がったまま
  で、その `ON DELETE CASCADE` に道連れにされていました
  （**`PRAGMA defer_foreign_keys` が遅らせるのは制約の検査であって、カスケードの動作では
  ありません**）。しかも返る `RecordFold` は `movedChildren: 0` で、呼び出し元からは
  損失が見えませんでした。

  子の参照列を一旦 NULL にして消える行から外し、削除のあとに元の値へ戻すようにしました
  （この間の宙ぶらりんは、遅延された検査が終端で見るときには解消しています）。外せない形
  （参照列が `NOT NULL`、子自身の主キーを兼ねている、`CHECK` で NULL を禁じている）でだけ
  子が失われ、その数を**削除の前後を実測して** `RecordFold.lostChildren` で伝えます。

## [0.17.0] - 2026-08-22

### 修正

- **届いた更新がローカルの別の行のセカンダリ UNIQUE に当たると、その相手からの同期が
  永久に止まっていました。** `applyUpdate` が畳みへ落ちるのはローカルに同じ id の行が
  無いとき（`applyInsert` へ委譲する経路）だけで、行がある場合は素の `UPDATE` を投げる
  だけだったためです。利用者が編集できる名前の列（`Tag.name` / `Classroom.name` /
  `Subtotal.name` / `Student.studentNumber` / `User.username` など）で、2 つの端末が
  独立に同じ名前へ辿り着くと起こります。

  例: A が `Tag(t1,"数学")` を `"国語"` へ改名し、B が独立に `Tag(t2,"国語")` を作成。
  B は同期のたびに `UNIQUE constraint failed: Tag.name` で例外を出し、その相手ぶんの
  取り込みが丸ごと巻き戻って `lastSeenId` が進まず、以後 A からは**競合と無関係な変更も
  含めて何も届かなくなります**。フルマージ経路（changelog ギャップ）でも同じことが起き、
  そちらは毎回ギャップを再検出して同じ所で落ち続けます。

  `applyInsert` が既に持っている解決（セカンダリ UNIQUE 違反を LWW で 1 行へ畳む）へ
  寄せました。作成の衝突と違い**更新対象の行はローカルに既に在る**ため、どちらが負けても
  実際に行が 1 つ消えます:

  - 届いた更新が勝つ場合 — 邪魔なローカル行を、更新される行へ畳んでから書き込みます。
  - ローカル行が勝つ場合 — 更新対象の行の方を勝者へ畳みます。届いた更新を黙って捨てると、
    相手は送り続けこちらは断り続けて**分岐したまま収束しません**。

  どちらの向きでも、消える行の子は先に生き残る行へ付け替えられ（0.14.0）、畳み先は
  `_tombstone.mergedInto` に載って他のクライアントへ伝わります（0.15.0）。
  1 回の更新が別々のユニーク索引で別々の行にぶつかる場合も、畳み切るまで繰り返します
  （**この繰り返しは 0.18.0 で不要になりました** — 相手を索引から先に全部引くため。
  0.18.0 を参照）。

- **タイムスタンプが同点のとき、両端末が逆向きに畳んで永久に振動していました。**
  別々の主キーの行どうしを比べる場面で、挿入側は同点を「ローカルが勝つ」、更新側は同点を
  「ローカルの相手が勝つ」として扱っていたため、**どちらの端末も自分の行を残す**判断に
  なっていました。非対称は挿入側に元からありましたが、更新側は今まで例外で落ちていたため、
  振動する形はこの版で初めて到達可能になったものです。

  例: A が時刻 T に `Tag(t1)` を `"国語"` へ改名し、B が同じ時刻 T に `Tag(t2,"国語")` を
  独立に作成。6 周回しても生き残る id が毎回 `t1` ↔ `t2` と入れ替わり、そのたびに新しい
  DELETE が `_changelog` に積まれ続けます（実測: 6 周で DELETE が 4 件ずつ）。

  親の LWW 比較を、子の付け替えが既に使っていた決定的な同点処理（`isPreferredOverRival`
  — 同点なら**主キーの辞書順**で決める）へ揃えました。挿入側と更新側の両方を同じ規則に
  したので、どの端末で解決しても同じ側が残ります。同点処理が成り立つ前提
  （**主キーは端末をまたいで一意（UUID/cuid）であること**）を README の制限事項と
  `SyncConfig.primaryKey` の doc に明記しました。

- **「相手が勝つ」で畳んだあとに更新が拒まれても、畳みが戻りませんでした。** ユニークが
  2 本以上ある表（`User(username UNIQUE, email UNIQUE)` など）では、1 回の書き込みが
  索引ごとに別々の相手へぶつかります。1 周目で相手 A に勝って A を削除し、再試行で
  2 本目の索引の相手 B に負けて `skipped` を返すと、**更新は拒まれたのに A は消えたまま**に
  なっていました（実測: `t(id, a UNIQUE, b UNIQUE)` の 3 行のうち、更新対象と巻き添えの
  1 行が両方消えた）。

  畳みを区切り（SAVEPOINT）の中で行い、負けが分かった時点で投げて**そこまでの畳みごと
  巻き戻す**ようにしました。投げた例外は `applyUpdate` 自身が受け止めるので、同期を止める
  側へは抜けません。巻き戻したうえで、更新対象の行だけを勝った相手へ畳んで収束させます。
  なお、ぶつかる相手を**畳む前に全部**評価する（`PRAGMA index_list` で索引を先に数える）
  形は別の段階として計画済みです（**0.18.0 で実装され、ここに書いた区切りと再試行の
  方式はもう存在しません** — 0.18.0 を参照）。

### 追加

- **`SyncResult.folds`** — 畳んだ一覧（表名・消えた id・残った id・その端末で実際に行が
  消えたか）。このライブラリは「ぶつかったら黙って畳む」ため、**何と何が 1 つになったかを
  利用者へ伝えられる**必要があります（例:「小計『知識・技能』が 2 つあったので 1 つに
  まとめました」）。三つ組は `_id_merge` に永続化されるものと同じ形なので、あとから
  見返す口も同じデータから作れます。

### 変更

- **畳みが `SyncResult` の数に出るようになりました。** 畳みは行が 1 つ消えるので
  `deleted` に数えます（畳んだ相手をそもそも持っていなかった場合は行が消えないので
  数えません）。ユニーク衝突を解決して取り込んだレコードは、挿入側と同じく
  `conflictsResolved` に数えます。従来は local-wins の畳みが `skipped` だけ、
  remote-wins の畳みが `updated` だけで、消えた行がどこにも出ていませんでした。
- `conflictsResolved` の説明を実態に合わせました（「UNIQUE 制約違反を UPSERT で解決した
  レコード数」→ 同一 id の UPSERT と、別 id・同一ユニークキーの行を 1 つへ畳んだぶんの
  両方を、届いたレコード 1 件につき 1 数える）。

`deleteProtected` の扱いは 0.16.0 のまま（畳みは保護対象外）です。旧バージョンの
クライアントと混在して動作します。

## [0.16.0] - 2026-08-21

### 変更（受け取り側の挙動）

- **届いた畳みを LWW で見送るようになりました。** ローカルの敗者行が畳みより後に更新
  されていれば、その畳みは適用されません。これまでは削除時刻を見ずに必ず適用していた
  ため、畳みのあとにユニークキーごと書き換えられて**もう衝突しない行**まで消えていました。
  見送ってもデータは失われず、その行を送り返した時点で相手側が逆向きに収束させます。
- **`deleteProtected` は畳みを保護しなくなりました。** 利用者操作による削除だけを止めます。
  畳みはユニーク制約が強制する統合であり、見送っても行は救えない（勝者行が届いた時点で
  `applyInsert` が同じ統合を行うだけで、それまでのあいだ子が宙に浮く）ためです。

どちらも旧バージョンのクライアントと混在して動作します。

### 修正

- 畳みの DELETE が、生まれた直後の changelog 掃除で消えることがありました。敗者 id に
  保持期間より古い削除履歴があると、その時刻で記録されて同じ `performSync` の
  `cleanupChangelog` に刈られ、`_id_merge` には記録が残るため二度と書き直されませんでした。
- `_changelog` / `_tombstone` が揃う前に `applyInsert` が使われたデータベースで、畳みが
  以降ずっと差分経路に載らなくなる問題を修正しました。
- 削除が実際には起きていない経路（`local_wins`）の tombstone に現在時刻を刻んでいたため、
  その id の到着が無条件に止まっていました。勝者行のタイムスタンプを刻むようにしました。
- フルマージ中（トリガー無効）に起きた畳みが差分経路に載らず、フルマージでしか他の
  クライアントへ渡らない問題を修正しました。主キーが動く 1:1 テーブルの付け替えも同様です。
- `_tombstone.deletedAt` の大小比較を `julianday` 正規化に統一しました（ISO-T 形式と
  スペース形式が混在するため）。

### パフォーマンス

- `PRAGMA foreign_key_list` / `table_info` の結果を `PRAGMA schema_version` をキーに
  データベース接続ごとにキャッシュするようにしました。畳みが一度でも起きたデータベースでは
  `applyInsert` のたびに外部キーを走査し、畳み 1 回ごとに全テーブルへ pragma を投げていました。

### 内部

- TypeScript 7 に備えて `module` / `moduleResolution` を `node16` へ移行しました
  （`node10` は TypeScript 6 で deprecated）。出力は CommonJS のままです。
- `package.json` に `type: "commonjs"` と `exports` を追加しました。`exports` の追加により
  deep import（`sqlite-nas-sync/dist/*`）は解決されなくなります。公開 API は
  `sqlite-nas-sync` からの import のみです。
- `npm run typecheck`（`tsconfig.test.json`）でテストコードも型検査するようにしました。

## [0.15.0] - 2026-08-20

- 畳み先を `_tombstone.mergedInto` に載せて他のクライアントへ伝播させるようにしました。
  競合を経験していないクライアントが、敗者行を消す前に子を畳み先へ引き取れます。
- 勝った側（`local_wins`）からも畳み先が通常の差分経路で届くようにしました。

## [0.14.0] - 2026-08-20

- 競合で負けた行を指している子を、勝った行へ引き取るようにしました（先に削除すると
  カスケードで子が道連れになっていました）。

## [0.13.1] - 2026-06-24

- INSERT / UPDATE の LWW 比較を `julianday` 正規化に統一しました。

## [0.13.0] - 2026-06-24

- tombstone を優先する LWW 削除統合と、リモートデータベースからの削除時刻解決を追加しました。

## [0.12.0] - 2026-06-12

- 競合解決 API（`applyInsert` / `applyUpdate` / `applyDelete`）を公開しました。

## [0.11.0] - 2026-06-11

- セカンダリ UNIQUE 違反を LWW で解決するようにしました。

## [0.10.0] - 2026-06-11

- `SyncResult` に `skippedRemotes` を追加しました。

## [0.9.0] - 2026-06-11

- リモートデータベースをローカルコピー経由で読むようにし、`lastSeenId` の更新を
  原子的にしました。

## [0.8.0] - 2026-04-11

### 破壊的変更

- テーブル自動検出 API に刷新しました（`discoverTables`）。

## [0.7.0] - 2026-03-27

- tombstone + heartbeat + フルマージによる zombie 問題の解決を追加しました。

## [0.6.1] - 2026-03-27

- stale なクライアントが NAS を汚染する問題を修正しました（pull-first フロー）。

## [0.6.0] - 2026-03-24

- コード構造をモジュールごとに整理しました。

## [0.5.0] - 2026-03-24

- `schemaVersion` 未指定時に、テーブルスキーマからハッシュを自動生成するようにしました。

## [0.4.0] - 2026-03-24

- `schemaVersion` オプションによるスキーマバージョン互換チェックを追加しました。

## [0.3.1] - 2026-03-23

- `better-sqlite3` を `peerDependencies` へ移しました。

## [0.3.0] - 2026-03-23

### 破壊的変更

- テーブルごとの設定（`timestampColumn` / `deleteProtected`）と `onAfterSync`
  コールバックを追加しました。

## [0.2.0] - 2026-02-07

- 最初のリリース。`_changelog` テーブルと SQLite トリガーによる差分同期。
